version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - npx next export
artifacts:
  files:
    - '**/*'
  base-directory: out
